# 2026-09-04 工作记录

## 背景
用户尝试完整跑通 CustomerServiceAgent，聊天报两类错：
1. `unknown command 'JSON.SET'`（Redis checkpoint）
2. `relation "refund_applications" does not exist`

用户误以为是「数据库没用户信息」——实际无关。

## 根因与修复

### 错1：Redis JSON.SET 失败
- 根因：localhost:6379 实际跑的是 Windows 原生老旧 redis **3.0.504（PID 5968）**，
  无 ReJSON 模块；docker 的 `ecommerce_redis`（redis-stack-server，带 ReJSON）被它抢占 6379。
- 修复（非破坏性，不动老 redis）：
  - `docker-compose.yaml`：redis 发布端口 `6379:6379` → `6380:6379`
  - `.env`：`REDIS_PORT=6379` → `6380`
  - 实测 `JSON.SET @6380` 成功。

### 错2：refund_applications 表缺失
- 根因：`app/models/refund.py` 定义了该表，但 5 个迁移文件无一创建它（模型-迁移脱节）。
  `alembic/env.py` 已 import RefundApplication 进 metadata，故可 autogenerate。
- 修复：`alembic revision --autogenerate -m "add_refund_applications"` 生成
  `migrations/versions/6a81cd1a4aba_add_refund_applications.py`（仅建表+3索引），
  已 `alembic upgrade head` 应用，表已存在。

### 错3（验证中发现的连带 bug）：政策答案发不出 + intent JSON 泄露
- 根因：`chat.py` 用 `token_sent` 标志门控政策答案下发；`intent_router` 的结构化输出
  token 流被当成普通 token 设了 `token_sent=True`，导致政策答案被 `if fallback_answer and not token_sent` 吞掉；
  同时 intent 的 `{"intent":"POLICY"}` 被泄露给前端。
- 修复：新增 `INTERNAL_LLM_TAG="internal_llm"`（`policy_answer_guard.py`），
  用 `.with_config({"tags":[...]})` 固化进 `intent_classifier` 与 `policy_answer_llm`；
  `chat.py` 对带 `POLICY_GUARD_TAG` 或 `INTERNAL_LLM_TAG` 的流事件直接丢弃。
  注：`.with_config` 比 `ainvoke(config=...)` 可靠——后者在 LangGraph 节点内标签不一定冒泡到 astream_events。
- 验证（临时 8001 实例跑通）：政策咨询返回完整答案且无 intent 泄露；订单查询流式正常（无回归）。

## 关键提醒（给用户）
- 用户正在跑的 8000 uvicorn 是改 `.env`/代码前启动的，内存里仍是 REDIS_PORT=6379 + 旧 chat.py，
  **必须重启**才能生效：`taskkill` 掉 8000 进程后重新 `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000`。
- 启动顺序：①`docker-compose up -d db redis`（redis 现走 6380）②`uv run alembic upgrade head`（已做过，表已建）
  ③`uv run python scripts/seed_data.py` ④`uv run python scripts/etl_policy.py` ⑤起 uvicorn ⑥起前端。

## 验证用过的结论（可复用）
- 直接对运行中 redis 测 JSON.SET：用 `redis.asyncio.Redis.from_url(...).json().set(...)`。
- alembic autogenerate 比对：env.py 的 target_metadata=SQLModel.metadata 已含所有 model，可自动发现缺表。
- 诊断 SSE 事件结构：写临时脚本跑 `graph.astream_events(..., version="v2")`，打印 `on_chat_model_stream` 的 `tags`/`content`，
  确认标签确实挂在事件 tags 上（intent_router 事件 content 为空，标签在 tags 里）。

## 数据库数据完整性检查（verify_db / verify_knowledge）
- 用户问「怎么检查数据库是否导入全部数据」。用 `uv run python scripts/verify_db.py` 与
  `scripts/verify_knowledge.py`，另配 psql 直查六表 count。
- 发现 `verify_db.py` 有 bug：第 11 行 `from app.models.order import Order, User`，但 `User` 实际在
  `app.models.user`，导致 `ImportError`。已改为分别 import（`Order` from order，`User` from user）并跑通。
- 当前 DB 真实状态：users=3、orders=5（与 seed_data.py 的 3 用户/5 订单一致，已全量导入）；
  knowledge_chunks=61（6 个政策源文件全覆盖，etl 已全量入库）；refund_applications/audit_logs/message_cards=0
  （这些表是运行时由退款流/聊天生成，非 seed，空属正常，不代表漏导）。
- 表名（供 psql 直查）：users / orders / knowledge_chunks / refund_applications / audit_logs / message_cards。

## 错4：Error while searching: No such index checkpoint_write
- 现象：单轮问答正常，但「同一会话线程的第二条消息」报 `No such index checkpoint_write`
  （用户连发「内衣能退吗」+「我要退 SN20240004」触发）。
- 根因：`AsyncRedisSaver` 依赖 Redis 的 `checkpoint` / `checkpoint_write` 两个搜索索引；
  包内源码 `setup()` 注释明确写 "It MUST be called before using the checkpointer"。
  而 `compile_app_graph()` 只 new 了 saver 就直接 compile，**从未建索引**。
  写入能成功（不依赖索引），但读取/搜索历史 checkpoint 需要索引 → 第二轮才炸。
- 修复（`app/graph/workflow.py`）：compile 前加 `await checkpointer.asetup()`
  （`asetup()` 创建两个索引，`create(overwrite=False)` 幂等，重复启动安全）。
  顺带清理该文件：删除未使用的 `import redis.asyncio as redis`，import 排序（ruff 现已全通过）。
- 验证（临时 8001 实例）：政策轮正常；同线程续问退货 → 「帮您提交退货申请。」
  索引错误消失，退货链路端到端跑通。回归测试 15 passed。
- 注意：此修复同样需要用户重启 8000 的 uvicorn 才生效。

## 错5：第三轮重复回答前两轮的问题（跨意图历史污染）
- 现象：连问「我的订单到哪了？」→「内衣拆封了能退吗？」→「我要退 SN20240004」，
  第三轮回答变成「针对您的三个问题，回复如下：…」，且用售后身份重新答了前两问。
  用户怀疑是 prompt 设置问题——**不是**，是会话状态结构坏了。
- 根因（已用真实图三轮复现坐实，checkpoint messages 直接取证）：
  `generate` 节点（ORDER / POLICY 路径）只返回 `answer`，**从不把 AI 回复写回 `state["messages"]`**；
  而 `messages` 是 `Annotated[list, operator.add]` 经 checkpoint 跨轮累积，`chat.py` 每轮都注入本轮 Human。
  于是第 3 轮 refund_agent 拿到 `[H1, H2, H3]` 三条连续 Human、中间零条 AI →
  模型认为三条都还没回，就一起答了（且用 REFUND_AGENT_PROMPT 的售后身份重答 Q1）。
- 修复（`app/graph/nodes.py`，三处）：
  1. `generate` 两个分支均回写 `AIMessage(content=answer)` 到 messages（核心，保证 Human/AI 交替）；
  2. 新增 `select_refund_context_messages()` + `MAX_REFUND_HISTORY_TURNS=3`：
     退款 Agent 只带最近 3 轮，且窗口起点固定为 `HumanMessage`
     （否则会拆开 `AIMessage(tool_calls)` 与 `ToolMessage` 配对，触发模型 API 400）；
  3. `REFUND_AGENT_PROMPT` 加「只回答最新一条消息 / 不越权答非售后问题」（第三道防线，治标）。
- 验证：六轮真实图复现（订单→政策→政策→政策→退款追问→退款补原因）：
  前四轮各自单独作答；第 5 轮只索要退货原因不复述旧问；第 6 轮正确调工具并保留退款流程上下文
  （拿到 SN20240001 + 原因，工具返回「含运动内衣不可退」，模型给出解释）。
- 测试：新增 `test/test_conversation_history.py`（6 用例，覆盖窗口裁剪/tool配对/AI回写/退款Agent窗口）。
  相关测试 21 passed，ruff 全通过。README 已补「多轮会话历史的一致性」章节。
- 注：复现脚本 thread 必须每次换新的，旧线程历史已被污染。

## 复用结论（补充）
- LangGraph Redis checkpointer：新建 `AsyncRedisSaver` 后必须 `await asetup()` 建索引，
  否则首次「读历史/多轮」会以 No such index 失败——且单轮测试不会暴露，必须测同 thread_id 多轮。
- 反复启停 uvicorn 可能触发 `asyncpg InterfaceError: connection is closed`（DB 池失效），
  属测试实例侧问题，干净重启即可，不代表项目 bug。
- **多轮 Agent 排查范式**：凡「前面几轮正常、某轮突然复述/串答」，先看 checkpoint 里 messages 的
  序列结构（Human/AI 是否成对、tool_calls 与 ToolMessage 是否配对），再看 prompt。
  可用 `graph.aget_state(config).values["messages"]` 直接取证。
- 从 Redis 直读 checkpoint 不可行：`channel_values` 是分片存储，读出来为空；
  取证请用 `graph.aget_state(config)`。

## 退款人工介入逻辑排查（只读，未改代码）
- 决策全在 `app/graph/tools.py` 的 `submit_refund_application` 工具（84-201 行）：
  1. 资格校验（`RefundEligibilityChecker`，纯硬规则）：订单属本人、状态 SHIPPED/DELIVERED、
     无重复申请、7 天期内、类别不在不可退名单 → 建 PENDING 申请（金额=order.total_amount）。
  2. 金额分流（阈值见 `app/core/config.py:84-85`）：
     - `< ¥500`：直接 APPROVED + `process_refund_payment.delay()` 立即打款，**不建 AuditLog、不通知人**。
     - `≥ ¥500 且 < ¥2000`：MEDIUM，建 AuditLog(PENDING)+`notify_admin_audit.delay()`，等人工 APPROVE 才打款。
     - `≥ ¥2000`：HIGH，同上（仅风险标记更高）。
- 人工审核入口：`POST /api/v1/admin/resume/{audit_log_id}`，动作 APPROVE/REJECT。
- 结论（用户「说退款秒退」的根因）：其订单 refund_amount < ¥500 → 资格过即自动放行，整条链路**无人介入**。
  这不是"风险被评足够低"，而是代码写死的金额阈值开关（`MEDIUM_RISK_REFUND_AMOUNT=500` 魔法数字，无风控模型支撑）。
- 关键审计性缺口（与银行四性之"可审计性"冲突，待改）：
  ① <500 自动退款**不写 audit_logs**，小额退款在审计系统是盲区；
  ② 自动打款=资金变动零人工复核零留痕；
  ③ ¥500/¥2000 阈值无依据、无配置说明；
  ④ 退款工具**不受痛点②的 Guardrail（policy_answer_guard）约束**——反幻觉护栏与资金安全是两回事；
  ⑤ 资格校验是形式校验（状态/时效/重复/类别），不查欺诈、退款频率、金额合理性、是否已退货收货。
- 依赖提示：`<500` 的 `process_refund_payment` 是 Celery `.delay()`，需 worker 运行才真正打款；
  当前为 mock（print 模拟）。worker 未起则申请置 APPROVED 但钱未动。

## Codex 安全复核：退款模块 9 项缺陷（已对照代码确认，read-only 未改）
- 全部属实。根因坐标：
  refund_service.py:80-93（去重只看 PENDING/APPROVED，漏 COMPLETED）、
  migrations/6a81cd1a4aba L41（order_id 仅非唯一索引，无唯一约束）、
  tools.py:143-157（<¥500 自动 APPROVED+立即打款、不写 AuditLog、不通知人）、
  refund_tasks.py:81-116（支付 task 无幂等/无流水表/transaction_id 仅返回值未落库）、
  admin.py:9/51/93（用 get_current_user_id 而非 get_admin_user_id，管理员鉴权失效）。
- 数据库脏数据：order_id=3 已有两笔 COMPLETED ¥199 重复退款（audit_logs=0），与"同订单两次退款"吻合。
- 约定修复优先级（用户指定）：
  ① 禁自动打款（全走人工审核）② 唯一约束+服务层拦 COMPLETED+并发 IntegrityError 兜底
  ③ admin 接口改用 get_admin_user_id ④ 支付 task 状态机+幂等键+流水表+持久化 txn_id
  ⑤ 全量审计+用户二次确认（拆 prepare/confirm 两工具）⑥ 风险分级后续。
- 注意：migration 6a81cd1a4aba 已应用，新约束须新建迁移（down_revision=6a81cd1a4aba），不可改旧文件。

## Codex 退款修复计划·设计评审（read-only 未改代码）
- 结论：方向正确可行，但需收敛+补洞。已核对代码逐条验证。
- 关键冲突：①唯一索引迁移遇 order_id=3 两笔 COMPLETED 会失败，须先去重再建索引（部分唯一索引只约束活跃态）。
  ②"admin.py 换 get_admin_user_id"关不掉伪造：admin_dashboard.py:40 客户端 mint admin token(is_admin=True)，
  且 security.get_admin_user_id 仅信任 JWT 的 is_admin claim、不回查 DB → 必须停用客户端 mint + 增加 DB 回查。
  ③RefundPayment(refund_application_id 唯一) 与"驳回后重开再批"冲突，须复用同一支付行。
  ④AuditLog.action 与 RefundApplication.status 双状态门控未明确退役（admin.py:61-63 靠 AuditLog.action 列任务）。
- 关键遗漏：⑤拆工具≠用户确认，须 LangGraph interrupt(HITL) 强制人工确认；⑥Celery 仍传 amount，应从 DB 读；
  ⑦Decimal 须全链路替换 float；⑧append-only 事件存哪张表未定；⑨现有测试引用旧状态改名后必红；
  ⑩支付任务"锁流水"应明确定义为锁 RefundPayment 行+条件更新。
- 过重项：DRAFT/PENDING_CONFIRMATION 持久化可裁剪，确认改"提交前闸门"即可，避免与唯一约束互锁。
- RefundStatus 改名波及 7 文件（tools/admin/refund_service/refund_tasks/models/__init__/test_v4_complete）+ emoji/notify 字符串。

## Codex 退款安全「第一轮」实现·代码审查（已逐文件核对实际改后代码）
- 第一轮改动已落地（迁移 b7d31a8c4e20 应用）。逐文件核对结论：
  - 管理员鉴权：security.get_admin_user_id 现回查 users.is_admin+is_active（security.py:147-154），**不再信任 JWT claim**；admin.py 两接口均 Depends(get_admin_user_id)；admin_dashboard 改用真实登录、删掉客户端 mint。比我上轮担心的更好。✅
  - 小额自动退款：tools.py 的 `<¥500` 自动 APPROVED+投递支付分支已删；所有金额统一建 AuditLog(PENDING)+返回"需人工审核"。✅（关掉了此前 audit_logs=0 的盲区）
  - 重复退款：refund_service 全状态拦截 + order_id 唯一约束（模型+迁移）+ IntegrityError 兜底；并发双创建时第二条被唯一约束拒绝。✅
  - 审批原子化：admin.py 用 with_for_update 锁 AuditLog+RefundApplication，action/status 非 PENDING 即 409；重复审批只成功一次。✅
  - 支付幂等：refund_tasks.process_refund_payment(refund_id) 仅收 id；APPROVED→PROCESSING→COMPLETED 条件更新抢占 + 失败回滚 APPROVED。✅ 但缺口：mock 支付 sleep(3) 期间 worker 崩溃被 Celery 重投，新任务见 PROCESSING→noop 直接返回，永不调 _complete，**退款卡 PROCESSING**。接真实支付前必修。
  - order_id=3：迁移用 CTE 归档重复行痕迹进保留行 + 重指 audit_logs + 删除重复物理行。对 mock 可接受，但与"不直接删除"原则有偏离；真实支付数据须改回 CANCELLED+部分唯一索引，禁止删除。
- 测试结论被夸大：全量 `pytest` 因 test/test_refund_subgraph.py 导入不存在的 app.graph.refund_subgraph 在收集阶段崩溃（整套件跑不出）。排除该坏文件后实得 **41 passed / 8 failed**（非总结说的 39/10）。
  - 8 失败=5 个 async 测试未标 @pytest.mark.asyncio（含 test_refund_rules 退款资格规则）+ 3 个需 LLM/网络的 v1_agent 用例，均与退款逻辑无关，属预存环境问题。
  - round1 的 6 个测试全是 mock 单元，**未断言**重复退款拦截/支付幂等/自动退款关闭；用户列 7 条验收标准基本无测试覆盖。"16 passed"≠"修复有效"。
- 余留（round-1 主动延后，合理）：LangGraph interrupt 用户二次确认、RefundPayment 流水+网关幂等键、Decimal、9 态状态机、append-only 事件表、部分退款、风控特征。其中"用户确认延后"理由成立（自动退款已关，Agent 最多建 PENDING 审核单，动不了资金）。
- 审查交付：纯文字评审，未改代码。下一步建议：①修 test_refund_subgraph（删或补模块）让套件可跑+补 3 条关键集成测试；②修 PROCESSING 卡死缺口；③文档标注 order_id=3 为一次性数据修复；④round-2 推进 RefundPayment/幂等键/Decimal/HITL。

## 2026-09-04 17:00 退款安全审查 v2（Codex 收敛方案 + 第一轮落地验证）

### 背景
Codex 把我上轮指出的 5 个冲突收敛进了第一轮（停用客户端 mint + DB 回查 / 接受双状态门控共存 / MVP 不建 RefundPayment / 唯一约束迁移先改 FK / 状态枚举不改），并落地实施。本次重新只读核对全部 10 个改动文件 + auth.py/config.py 调用点，验证方案 vs 实际是否一致。

### 方案 5 项 vs 实际落地（一致表）

| # | Codex 方案承诺 | 实际代码 | 评估 |
|---|---|---|---|
| 1 | 管理员鉴权回查 DB is_admin+is_active | security.py:147-154 `await session.get(User, ...)` + `not user.is_admin or not user.is_active` → 403 | ✅ |
| 1 | 停用客户端 create_access_token(is_admin=True) | admin_dashboard.py:30-50 改调 `/api/v1/login`；payload 校验 `is_admin=True` 才存 token | ✅ |
| 2 | 删除 <¥500 自动 APPROVED+投递支付任务 | tools.py:142-188 全走 PENDING + AuditLog(PENDING) + notify_admin_audit.delay() | ✅ |
| 3 | order_id 唯一约束（物理 UNIQUE，MVP 不做部分索引） | refund.py:31 `UniqueConstraint("order_id", ...)` + migration b7d31a8c4e20 | ✅ |
| 3 | 服务层全状态拦截 + IntegrityError 并发兜底 | refund_service.py:166-172 + 203-211 | ✅ |
| 4 | 审批事务内行锁 AuditLog + RefundApplication | admin.py:107-135 `with_for_update()` × 2 | ✅ |
| 4 | 状态双检 + 409 区分已处理 | admin.py:118-122 (audit.action) + 138-142 (refund.status) | ✅ |
| 4 | commit 后才投递 task | admin.py:184 `await session.commit()` → 189 `.delay()` | ✅ |
| 5 | 支付 task 只接 refund_id，从 DB 读 amount | refund_tasks.py:74-99 `update...RETURNING refund_amount` | ✅ |
| 5 | 原子 APPROVED→PROCESSING 抢占 | refund_tasks.py:84-95 条件 UPDATE，rowcount 守门 | ✅ |
| 5 | 失败回滚 PROCESSING→APPROVED | refund_tasks.py:126-152 `_restore_after_failure` | ✅ |
| 5 | 重复执行 noop（PROCESSING/COMPLETED） | refund_tasks.py:101-105 + 156-157 | ✅ |

### 上轮 5 个冲突解决情况
1. ✅ 唯一索引迁移遇脏数据 → 用 CTE 先改 audit_logs.refund_application_id 指向 kept_id → 拼 admin_note 保留痕迹 → DELETE → DROP index → UNIQUE
2. ✅ 客户端伪造 token → 双层堵（dashboard 改登录 + security 回查 + 测试 403 验证）
3. ✅ RefundPayment 唯一 vs 驳回重开 → MVP 不建表，绕过
4. ✅ 双状态门控 → 接受双写（admin.py L118/L138 同步），第一轮不重构
5. ✅ RefundStatus 改名 → 状态枚举保留 PENDING/APPROVED/PROCESSING/REJECTED/COMPLETED/CANCELLED

### 新发现 A 级遗留（必须进 round-2/3）
- **A1** `create_access_token` 仍 mint `is_admin` 装饰字段（security.py:29）。虽不再被任何鉴权依赖，但浪费 token 空间+误导后来人。第二轮建议从 to_encode 中删除。
- **A2** `admin_comment` 拒绝时**后端不强制**。仅 admin_dashboard.py:405 前端校验，API 绕过即可不带原因拒绝 → 审计不完整。admin.py:36-37 改 Pydantic validator：`action=REJECT` 时 admin_comment 必填。
- **A3** admin.py:189-198 commit 后派发 task 失败 → 状态 APPROVED 卡死，无 sweeper。`dispatch_errors` 仅写 decision_metadata，无自动重投。短期可接受（人工可见），第二轮必修 transactional outbox。
- **A4** refund_tasks.py:161 `time.sleep(3)` 期间 worker 崩溃 → PROCESSING 卡死。Celery 重投时新任务看到 PROCESSING → noop（设计正确），但没人把 PROCESSING 推进 COMPLETED 或回退 APPROVED。需 sweeper job 扫描 PROCESSING 超时（>N 分钟）→ 走失败路径。
- **A5** RefundPayment 流水表 / 网关幂等键 / provider_transaction_id 持久化——接真实支付网关前必修，否则重复打款风险仍在（mock 阶段无外部资金，问题被掩盖）。
- **A6** LangGraph interrupt 强制用户二次确认——Codex 评估"round-1 关闭自动退款后优先级降低"成立，但接真实支付后必做。
- **A7** `tools.py:141` `float(refund_app.refund_amount)` + refund.py:60 `refund_amount: float`——全链路 Decimal 改造推迟到接真实支付前。
- **A8** `tools.py:167` context_snapshot 只 `{order_sn, reason}`，admin 工作台看不到订单金额/商品列表（admin_dashboard.py:343 取 order_data 拿不到）。UX 缺陷非安全。

### 演示/作品口径（重要）
第一轮落地后**仍不能说**"可安全执行真实资金退款"，因为 A5/A6 未做。
正确口径："已实现退款申请+人工审核流程的安全加固（自动打款拦截、重复退款防护、管理员越权防护、任务幂等）。接真实支付网关前需补 RefundPayment 流水表、网关幂等键与强制用户二次确认。"

### 测试覆盖评价（关键差异）
用户报告"针对性测试 16 passed"——但 test_refund_security_round1.py 只有 6 个函数，16 应包含其它手工 curl/集成验证。**round-1 必做 5 项中只有 A（管理员鉴权）有 mock 单元断言**；B（自动打款关闭）/C（重复退款 DB 约束）/D（审批原子化）/E（支付幂等）均无针对性集成测试。Codex 7 条验收标准里，**只有"伪造 claim 403"和"已 COMPLETED 重跑 noop"**有断言。建议 round-2 补 5 条集成测试（DB 真实跑 + concurrent 提交）。

### 审查结论
- 方向 ✅ 方案 v2 收敛合理（5 冲突全解决、过重项裁剪、interrupt 暂缓理由成立）
- 落地 ✅ 实际代码与方案承诺一致，未发现缩水/走样
- 测试 ⚠️ 6 单元 + 10 手工 ≈ "验证通过"，但**未覆盖**重复退款 DB 拦截、审批并发、支付幂等的端到端
- 收尾 ✅ 全部 10 文件已读、未改代码；memory 已追加审查记录

## 2026-09-04 17:40 退款安全审查 v3（A1/A2/A4/A8 修复）

### 逐项核对
- **A1** ✅ create_access_token 去掉 is_admin 参数（security.py:16-29），to_encode 无 is_admin；auth.py:90/145 已同步 `create_access_token(user_id=user.id)`；TokenResponse 仍返回 is_admin（从 DB 读，供前端判断）；admin_dashboard 的 `payload.get("is_admin")` 仍工作。测试 test_access_token_contains_no_role_claim 断言 "is_admin" not in payload。
- **A2** ✅ AdminDecisionRequest 加 model_validator(mode="after") 的 reject_requires_comment（admin.py:39-43），REJECT+空白→ValueError→422。后端强制。测试 test_admin_decision_requires_comment_for_rejection 覆盖。
- **A4** ✅ recover_stalled_refunds（refund_tasks.py:178-233）：updated_at<cutoff(10min) + FOR UPDATE SKIP LOCKED 防并发；恢复 APPROVED+写 audit metadata+re-delay；re-delay 失败保留 APPROVED。beat_schedule 60s（celery_app.py:28-33）；docker-compose 加 celery_beat；start.sh 加 beat；config.py:86 加 REFUND_PROCESSING_TIMEOUT_MINUTES=10。
- **A8** ✅ context_snapshot 改 {question, order_data{order_id/order_sn/status/total_amount/items}, refund{...}}（tools.py:167-181），匹配 admin_dashboard 渲染逻辑——顺带修复"tools 传 {order_sn,reason} 但 dashboard 读 question+order_data"的隐藏断层。

### 新发现
- **P1（真实支付致命）**：A4 恢复有双 PROCESSING 竞态——recover 把 PROCESSING→APPROVED 并 re-delay 后，若原 worker 还活着（慢），其 _complete 的 `UPDATE WHERE status=PROCESSING` 仍成功（新 worker 已重新 claim 成 PROCESSING），导致 mock 重复 print、真实支付**重复打款**。必须与 A5（RefundPayment+网关幂等键+CAS/版本号）一起上。
- **P2**：test_stalled_refund_recovery_is_scheduled 只断言 beat_schedule 注册任务名，**未测 _recover 核心逻辑**（超时/恢复/重投/审计）。A4 核心零测试覆盖。
- **P3**：temp.py 根目录残留旧 customer_ui.py 草稿（_init_token 直接 mint token），属垃圾文件应删。
- **P4**：config.py 重复定义 SECRET_KEY/ALGORITHM/ACCESS_TOKEN_EXPIRE_MINUTES（L56-59 vs L96-98）+ 重复 model_config（既有脏代码）。

## 2026-09-04 16:00 落实 P2/P4 + 文档更新（用户拍板：A5 暂缓）
- 用户决策：A5（RefundPayment 流水表 + 网关幂等键）暂缓，接真实支付前再做（已在 README 如实声明边界）。
- P2（补 A4 核心测试）：test_refund_security_round1.py 新增 3 用例，覆盖 recover_stalled_refunds 的
  ①超时恢复 PROCESSING→APPROVED + 审计写 payment_recovered_by + re-delay；②requeue 失败捕获（保留 APPROVED）；
  ③空列表 noop（不 re-delay）。测试手段：monkeypatch DatabaseTask.run_async→asyncio.run + 假 session + 替换
  process_refund_payment.delay。结果 12 passed（原 9 + 新 3）。
- P4（config.py 去重）：整体重写，删除重复的 SECRET_KEY/ALGORITHM/ACCESS_TOKEN_EXPIRE_MINUTES（两处）与
  三份 model_config，收敛为一份 SettingsConfigDict(env_file=".env", extra="ignore")——与原最终生效配置一致
  （env_ignore_empty=False，原 L52 的 env_ignore_empty=True 从未生效）。字段 28 个齐全、实例化正常、测试无回归。
- 文档：README 修正两处过时描述（L11「风险分级转人工」、L19「大额才人工审核」→「统一人工审核」），
  新增「退款安全（资金操作全人工 + 幂等防重）」小节（四层防护 + 如实声明 mock 支付边界）。
- 未动：temp.py（P3 残留垃圾，本次未在范围内）。

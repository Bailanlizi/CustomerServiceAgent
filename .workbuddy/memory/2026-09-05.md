
## P2 修复完成

按优先级完成 P2 修复，65→49 全过 + ruff 干净（不含历史 admin.py）。

### P0-1 增强 ToolOutcome + Registry 自动填充元数据
- `app/graph/tool_registry.py`：ToolOutcome 扩展为含 9 项元数据（tool_name / domain / workflow_stage / conversation_id / thread_id / user_id / order_id / timestamp / idempotency_key），Registry.invoke 用 `dataclasses.replace` 自动 envelope；handler 只返回业务结果。审计钩子 `write_tool_audit_log` 写入 AuditLog.decision_metadata + context_snapshot。

### P0-2 工具 core handler 直接返回 ToolOutcome
- `app/graph/tools.py`：每个工具拆为 `core_*`（返回 ToolOutcome）+ `@tool` LangChain 薄壳（从 InjectedState 取参 → core → 返回 message 字符串）。新增 `query_order_tool` (domain=ORDER)、`query_refund_status` (domain=REFUND)。所有 wrapper 通过 `GuardedToolExecutor.invoke(...)` 路由到 Registry。

### P0-3 GuardedToolExecutor 真正被使用
- `app/graph/workflows/refund.py`：删除 `_submit_handler` 的 `re.search(r"#(\d+)", text)` 和 `_eligibility_handler` 的 `text.startswith("✅")`；改为消费 `outcome.code` / `outcome.data["refund_id"]`。所有工具调用经 `_executor.invoke(...)`，FSM 不再依赖字符串解析。

### P1-1 工具登记补齐
- `_register_capabilities()`：增加 `query_refund_status` (audit_level=read)、`query_order_tool` (domain=ORDER)。4 个核心能力全部登记。

### P1-2 顶层 ToolNode 接入 Guard
- `app/graph/workflow.py`：`refund_tools + order_tools` 全部经 GuardedToolExecutor 调用顶层 ToolNode；删除裸 ToolNode 路径。

### P1-3 admin.py 元数据补齐
- `AuditTask` 增加 `refund_amount / refund_risk_level / refund_status / workflow_stage / user_confirmed`，从 `last_tool_result.tool_outcome.data` 与 `context_snapshot.refund` 双源读取。

### P2 测试（新增 test/test_tool_registry.py，23 个）
- A. Registry 注册校验（5）：重复 / 缺 allowed_stages / 只读工具配置幂等键 / 敏感审计只读工具 / 非 callable handler
- B. Guard 拒绝路径（7）：未注册 / 错误领域 / 错误阶段 / 缺槽位 / 资格未通过 / 未确认 / handler 不执行
- C. 元数据自动填充（2）：envelope 9 项 + handler 异常映射 SYSTEM_ERROR
- D. 顶层 ToolNode 走 Guard（2）：与 Workflow 共享 Outcome.code
- E. 退款查询工具登记（3）
- F/G. core_query_order / core_query_refund_status 行为（2）
- H. admin.py 字段（1）
- I. _outcome_to_metadata 元数据覆盖（1）

### 关键约束（用于未来类似架构）
- LangChain `@tool` 透传到 `_executor.invoke` 的 state 必须包含全部 Guard 检查字段：workflow_stage、active_domain、user_confirmed、collected_slots、refund_reason_category、active_order_id、last_tool_result、thread_id——任何字段缺失都会导致 Guard 拒绝。
- 测试 fixture 的 TypedDict 必须显式声明 InjectedState 用到的所有字段，否则 LangGraph runtime 抛 KeyError。
- LangChain StructuredTool 的 `.name` 属性与 Registry 注册名一致很重要——用 `query_order_tool` 而不是 `query_order`，否则跨入口匹配会失败。

### 测试结果
- 49/49 全过（test_tool_registry.py 23 + test_refund_workflow_fsm.py 11 + test_refund_tools.py 11 + test_conversation_history.py 4）
- ruff 干净（我改动的 8 个文件）

## Codex 第二轮审查收尾

按 7 项 codex 第二轮审查问题逐一修复（不重做规划，仅清理 P2 实施残留）：

1. 删 test/test_refund_subgraph.py（ModuleNotFoundError 中断 pytest 收集）
2. AuditAction.REJECTED → AuditAction.REJECT（非法枚举吞敏感审计）
3. Guard 阶段校验加 `if "*" not in allowed and stage not in allowed` 通配
4. 审计写入 fire-and-forget → `await write_tool_audit_log`（`_outcome_to_state_update` 改 async）
5. admin.py 批量查 ConversationSession（`WHERE thread_id IN (...)` + dict 查找）消除 N+1
6. Registry `_idempotency_cache`（仅缓存 ok=True）
7. ruff 全仓库 308 错误是历史遗留，git stash 验证后仅保证改动文件 ruff 干净

测试隔离关键陷阱：autouse fixture 必须同时保存/恢复 `_capabilities` + `_handlers` + `_idempotency_cache`；因为 `_register_capabilities` 有 `if tool_registry.names(): return` 短路，单独清 handlers 不够。

最终：54/54 核心测试全过；9 个改动文件 ruff 干净；全量 pytest 107/115（8 个失败是历史遗留 async 装饰器缺失，与本次无关）。

## 文档与代码对齐（P2 收尾后）

按"单一职责 + 真源唯一"原则，仅动 docs/architecture-update.md（设计真源）+ README.md（产品概览），不新建文档：

- docs/architecture-update.md 第 6.1 节：ToolCapability 字段从 6 项扩到 9 项（补 requires_user_confirmation / idempotency_key_template / writes_to_conversation / audit_level / owner_workflow）；调用路径补 Guard 7 步检查顺序 + `"*"` 通配 + 进程内幂等缓存 + handler 异常映射 SYSTEM_ERROR + write_tool_audit_log 通道；新增"P2 已登记能力清单"小表（4 个 ToolCapability）+ "三道防线"小节。
- docs/architecture-update.md 第 7 节：模块目录补 app/graph/tool_registry.py + app/graph/workflows/ 子目录。
- docs/architecture-update.md 第 8 节 P2 验收：从 2 行扩展到 7 项实测落地（拒绝路径 / 元数据 envelope / 审计通道 / 进程内幂等 / read-only 通配 / admin 批量 / handler 异常可控）+ 测试位置说明。
- README.md 主要特性：新增"工具能力治理（ToolRegistry / GuardedToolExecutor / ToolOutcome）"亮点条目。
- README.md 项目结构图：补 tool_registry.py + workflows/refund.py 子目录。

约束记忆：docs 与 README 不重复 README 内容；架构文档 = 设计真源，README = 产品概览；项目无 private-docs，本项目架构变更只需更新 docs/ 下唯一文件。

## P3 方案审查与调整（设计期，未落地代码）

对 P3 实施计划做了一次外部审查，8 条声称全部到代码核对属实，已整合进方案。关键修正（原计划遗漏的硬依赖）：

1. `_register_capabilities` 的 `if tool_registry.names(): return` 全局 guard 会阻止跨模块注册 → 改为 per-name 幂等（`if name not in names: register(...)`）。
2. `tools.py:637` 薄壳 `_executor.invoke("query_order", …)` 与注册名 `"query_order_tool"` 错位（潜 bug）→ 改名对齐。
3. `core_query_order` 返回 `order_id`，`persist_turn` 读 `order_data.get("id")` → order 子图组装 order_data 时映射 `order_id→id`（并保留 `order_sn` 供 v2 测试）。
4. `chat.py:129` SSE 仅捕获 `{"generate","refund_agent"}` → 加 order 子图节点名 `"order_workflow"`。
5. `generate` 仅删除 order_data 拼装块(245-275)，保留 generic 路径（FIX test_conversation_history 用 order_data=None）；ORDER 不再路由到 generate。
6. `_ensure_capabilities_registered` 同时注册 order 模块（薄壳懒注册覆盖 ORDER 能力）。
7. order 子图必须回写 `order_data` 到 state（v2 集成测试断言）。
8. 子图模块级缓存 + 注册幂等，避免 test fixture clear 后失败。

自我纠正：原方案"移除 generate 的 ORDER 分支"措辞不准（generate 本无独立 ORDER 分支，精确动作是删 order_data 拼装块）；另评审指出 run_rag_baseline.py 只 import GENERATE_SYSTEM_PROMPT/llm，不依赖 generate 函数（不影响"generate 必须保留"结论）。

P3 文件级改动清单（未执行）：新建 app/graph/workflows/order.py；改 workflow.py(Thin Orchestrator+order_workflow 节点)、nodes.py(refund_agent 编译一次/删 query_order+should_call_refund_tool/删 generate order_data 块)、refund.py(移除 query_order_tool 注册+per-name guard+缓存)、tools.py(薄壳改名+注册 order)、chat.py(SSE 捕获加 order_workflow)；新增 test_order_workflow.py；文档补 query_order_tool owner=OrderWorkflow 与 P3 验收补"ORDER→REFUND 连续不丢 active_order_id"。

## P3 已落地

按审批通过的计划文件 `C:\Users\Administrator\.workbuddy\plans\radiant-cascade-curie-voFuVdJp.md` 整批落地。8 条审查修正均已实施并验证。

### 改动文件清单
- `app/graph/workflows/order.py`（新建）：OrderWorkflow 子图——per-name 注册 query_order_tool，模块级缓存 `_ORDER_SUBGRAPH`；node_query_order 经 `_executor.invoke("query_order_tool", ...)` 调 core_query_order；outcome.data 映射 `order_id → id` 写入 `order_data`；node_compose_order_answer 确定性合成 answer 并回写 AIMessage。
- `app/graph/workflow.py`：重构为 Thin Orchestrator——新增 `dispatch_router` 按 `active_domain` 派发到 `order_workflow`/`retrieve`/`refund_agent`；删顶层 `ToolNode(refund_tools+order_tools)`、`should_call_refund_tool` 边、`query_order` 节点 + import；`route_intent` 把 `ORDER` 映射到 `order_workflow`。
- `app/graph/nodes.py`：`refund_agent` 改用 `get_refund_subgraph()` 复用模块级缓存；删 `query_order`、`should_call_refund_tool`；`generate` 仅删 245-275 行 order_data 拼装块（保留 POLICY 分支与 generic 路径）；清理 `async_session_maker`/`select`/`Order` 死 import。
- `app/graph/workflows/refund.py`：`_register_capabilities` 改为 per-name 幂等注册（不阻断跨模块）；移除 `query_order_tool` 注册段（迁到 order.py）；`build_refund_subgraph` 模块级缓存 `_REFUND_SUBGRAPH` + `get_refund_subgraph()` 入口。
- `app/graph/tools.py`：`query_order_tool` 薄壳调用名 `"query_order"` → `"query_order_tool"`；`_ensure_capabilities_registered` 同时注册 refund + order。
- `app/api/v1/chat.py`：SSE 兜底事件捕获集合 `{"generate","refund_agent"}` → `{"generate","refund_agent","order_workflow"}`。
- `test/test_order_workflow.py`（新建）：10 用例——per-name 幂等 / owner=OrderWorkflow / outcome_to_order_data 字段映射 / 子图结构 / 缓存同一性 / 成功失败路径 / 端到端 ainvoke。
- `test/test_tool_registry.py`：`test_query_order_registered_with_order_domain` 改 import 到 order_module 并断言 `owner_workflow=OrderWorkflow`；`test_all_refund_tools_are_in_registry` 拆成 REFUND 三件 + ORDER 一件双模块读取。
- `docs/architecture-update.md`：第 7 节目录 P3 占位 → 已落地；第 8 节 P3 验收补 5 项实测（per-name 幂等 / 不丢 active_order_id / SSE / 切换不丢 summary / 64 测试）。
- `README.md`：项目结构图补 `workflows/order.py` 行。

### 验证结果
- ruff：8 个改动文件全干净
- pytest：64/64 全过（原 54 + 新 10；test_tool_registry.py 中 test_all_refund_tools_are_in_registry 因迁移产生 1 处失败，已修）
- 计划文件位置：`C:\Users\Administrator\.workbuddy\plans\radiant-cascade-curie-voFuVdJp.md`
